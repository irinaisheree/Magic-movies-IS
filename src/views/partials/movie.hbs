  <div class="flip flip-vertical">
                        <div class="front">
                            <img src="{{poster}}"
                                alt="{{title}}">
                        </div>
                        <div class="back">
                            <h1>{{title}}</h1>
                            <h2>Genre: {{genre}}</h2>
                            <p>Description: {{description}}</p>
                            <a href="/details/{{_id}}" class="details">Details</a>
                             <a href="/details/{{_id}}/attach" class="details">Attach</a>
                        </div>
                    </div>